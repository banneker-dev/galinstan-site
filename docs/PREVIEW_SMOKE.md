# Preview deploy smoke test

Pushed to exercise the real Cloudflare credentials, the wrangler action and the
Pages project on a preview URL before `site-2026.09.1` touches the apex. A release
process first used under pressure is a release process that fails under pressure.

This file is not in the build allowlist, so it reaches the repository and not the
web — which is itself the thing worth confirming on this run.
